# Rewrite all URLs to the index page
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /portal/noext/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . index.php [L]
</IfModule>
